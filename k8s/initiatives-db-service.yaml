apiVersion: v1
kind: Service
metadata:
  name: initiatives-db
  labels:
    app: mssql
    product: octava
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 1433
  selector:
    app: mssql
    product: octava
    tier: backend
